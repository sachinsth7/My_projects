numbers = [10, 20, 30, 40]
names = ["Sachin", "Samar", "Priya"]
mixed = [1, "Data", True, 3.5]



print(numbers[1])

numbers.append(60)
print(numbers)
numbers.remove(60)
numbers.remove(20)
for i,n in enumerate(numbers):
    print(i,n)


employee  = {
    "name" : "Sachin",
    "Age" : 26,
    "department": "Data Engineering"
}

print(employee.get("Age"))

employee["Age"] = 28

employee["salary"] = 500000

for key, value in employee.items():
    print(f"This is the key->{key} and this is the value of it :{value}")



employees = [
    {"name": "Sachin", "Age": 27, "salary": 50000},
    {"name": "Rajendra", "Age": 34, "salary": 20000}
]


for emp in employees:
    print(emp['name'])


high_salary = [e for e in employees if e["salary"] > 40000]
print(high_salary)

total = sum(e["salary"] for e in employees)
print(total)

employees = [
    {"name": "Sachin", "Age": 27, "department": "Science", "salary": 50000},
    {"name": "Rajendra", "Age": 34, "department": "Astrology", "salary": 20000},
    {"name": "Guru", "Age": 34, "department": "Management", "salary": 23400},
    {"name": "Sandy", "Age": 56, "department": "President", "salary": 10000}
]

for emp in employees:
    print(f"Employee {emp["name"]} works in {emp["department"]} and earns ${emp["salary"]}")

average_salary = (sum(e["salary"] for e in employees))/ len(employees)
print(f"The average salary is: ${average_salary: .2f}")


employee_ids = [101, 102, 103, 101, 104, 102]
unique_eid = set(employee_ids)
print(unique_eid)

store_a = {"TV", "Laptop", "Camera"}
store_b = {"Laptop", "Phone", "Camera", "Tablet"}

common = store_a | store_b
print(common)

print(store_a & store_b)

print(store_a - store_b)


employee = {
    "name": "sachin",
    "department" : "data_engineering",
    "salary" : 53000
}

print(employee.keys())


for key, value in employee.items():
    print(key, value)


print(employee.get("salary"))


employee['age'] = 28

print(employee['age'])


employee.pop('age')

print(employee)

dict1 = {"a": 1, "b": 2}

merged = employee | dict1
print(merged)

employees = [
    {"name": "Sachin", "dept": "Data", "salary": 50000},
    {"name": "Priya", "dept": "Analytics", "salary": 60000},
    {"name": "Samar", "dept": "Cloud", "salary": 55000}
]


for e in employees:
    e['salary_usd'] = e['salary']/ 140

print(employees)


emp_basic = {"id": 101, "name": "Sachin", "department": "Data Engineering", "location": "st.louis"}
emp_details = {"age": 27, "salary": 50000, "location": "St Louis"}

emp_full = emp_basic | emp_details

emp_full['status'] = 'Active'
emp_full.pop('location')

print(emp_full)


for key, value in emp_full.items():
    print(f'{key} -> {value}')

import json

print(json.dumps(emp_full, indent = 2))



json_object = json.dumps({
  "name": "Sachin",
  "age": 27,
  "skills": ["Python", "SQL", "ETL"],
  "active": True
}, indent= 4
)


d = json.loads(json_object)
print(type(d))

product = {
    "id": 1001,
    "name": "Laptop",
    "price": 1200,
    "in_stock": True,
    "specs": {"ram": "16GB", "storage": "512GB SSD"}
}
prod = json.dumps(product, indent = 2)
print(prod)

products = json.loads(prod)
products['discount'] = 0.1
print(products)

def flatten_dict(d, parent_key="", sep="."):
    items = []
    for k, v in d.items():
        new_key = f"{parent_key}{sep}{k}" if parent_key else k
        if isinstance(v, dict):
            items.extend(flatten_dict(v, new_key, sep=sep).items())
        else:
            items.append((new_key, v))
    return dict(items)

data = {
    "id": 1,
    "name": "Sachin",
    "contact": {"email": "sachin@example.com", "phone": "555-1234"},
    "skills": ["Python", "SQL"]
}

flat = flatten_dict(data)
print(flat)


test = {
  "id": 1001,
  "name": "Sachin Shrestha",
  "contact.email": "sachin@example.com",
  "contact.phone": "555-1234",
  "orders": [
      {"id": 5001, "amount": 120.5},
      {"id": 5002, "amount": 85.0}
  ]
}


for key, value in test.items():
    trail = []
    if isinstance(value,list):
        for items in value:
            trail.append((items['id'], items['amount']))
print(trail)


